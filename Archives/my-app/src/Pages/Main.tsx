import {ReactElement} from 'react';
import PageLayout from 'Modules/PageLayout';

const Main = (): ReactElement => {
    return (
        <PageLayout>
            <div>Главная</div>
        </PageLayout>
    )
};

export default Main;